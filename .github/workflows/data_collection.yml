name: Eternal Return Data Pipeline

on:
  schedule:
    # 한국 시간 기준 01:00 ~ 09:00
    # UTC 기준 16:00 ~ 00:00
    - cron: '*/10 16-23,0 * * *'
  workflow_dispatch:

# 같은 수집 작업이 동시에 두 개 실행되지 않도록 방지
concurrency:
  group: er-makgeumgu-collector
  cancel-in-progress: false

jobs:
  collect-data:
    runs-on: ubuntu-latest

    permissions:
      contents: write

    steps:
      - name: 📥 리포지토리 체크아웃
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: 🐍 파이썬 환경 세팅
        uses: actions/setup-python@v5
        with:
          python-version: '3.9'

      - name: 📦 필요 라이브러리 설치
        run: |
          pip install requests

      - name: 🚀 데이터 수집 스크립트 실행
        env:
          BSER_API_KEY: ${{ secrets.BSER_API_KEY }}
        run: |
          python collect_data.py

      - name: 💾 수집 데이터 및 Snowball 상태 자동 커밋 & 푸시
        run: |
          git config --local user.email "action@github.com"
          git config --local user.name "GitHub Action Bot"

          git add \
            er_makgeumgu_dataset_v2.csv \
            snowball_pending_add.txt \
            snowball_processed.txt \
            known_games.txt

          if git diff --cached --quiet; then
            echo "변경사항이 없습니다."
            exit 0
          fi

          git commit -m "🤖 [자동] 막금구 데이터 수집 업데이트"

          # 수집하는 동안 main에 다른 커밋이 들어왔을 경우
          # 최신 원격 변경사항을 현재 수집 커밋에 반영합니다.
          git fetch origin main
          git rebase origin/main

          # rebase가 끝난 최신 상태를 push
          git push origin main
```
